server {
    listen 80;
    server_name cloud.projecloud.cat;

    location / {
        proxy_pass http://127.0.0.1:8080;  # frp's vhostHTTPPort (see below)
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
    }
}
